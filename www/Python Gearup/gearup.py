def remove_duplicates(lst: list) -> list:
    """removes duplicates from the given list, and returns that as a new list"""
    pass

def invert_dict(dict_to_invert: dict) -> dict:
    """returns a dict with the original dict's keys and values switched"""
    pass

# Discuss: If keys can't contain duplicates, why does keys need to be a list?
# Why does keys need to be a list? Why can't it be a set - it can't contain duplicates?
def lists_to_dict(???) -> ???:
    """returns a dict with the given keys and values
       if keys and values are different sizes, returns the empty dict
       if keys contains duplicates, returns the empty dict"""
    pass
