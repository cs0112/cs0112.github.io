def cast_of_chars(txt: str) -> set:
  '''Given a string of text, find all the words that begin with 
  a capital letter. Return a set (no duplicates) containing those 
  words, converted to all-caps. 

  After writing examples: we realized we need to say "remove punctuation"
  '''

# INTERMEDIATE SOLUTION, END OF CLASS
# For complete solution, see notes. 

  # break up the string by spaces
  words = txt.split()  
  cleaned = []
  for word in words: 
    # remove punctuation
    # TODO ???
  for word in cleaned:
    # filter out uncapitalized 
    # TODO ???

  # remove punctuation
  # filter out uncapitalized words
  # full-caps the result

  caps_words.add(word)
  cleaned.add(word) 
  
  
  for word in caps_words: 
  
  all_caps.add(word) 



  pass













###############################################################
# Not everything we need, but useful pieces. Put them in order,
#   but keep in mind there may be "holes" -- don't worry about 
#   that yet.


